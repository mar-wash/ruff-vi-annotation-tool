# Server
# Port for the local web server.
PORT=3000

# Runtime environment name.
NODE_ENV=development

# Database
# Path to the SQLite database file (created automatically on first run).
DATABASE_PATH=./data/ruffvi.db

# Admin
# Set this to any secret string to protect admin routes and /admin/import uploads.
ADMIN_SECRET=replace-with-a-secret-value
